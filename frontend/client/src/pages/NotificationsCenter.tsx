/**
 * NOTIFICATIONS CENTER PAGE
 * Consolidated - redirects to NotificationCenter
 */

import NotificationCenter from "./NotificationCenter";

export default function NotificationsCenter() {
  return <NotificationCenter />;
}
