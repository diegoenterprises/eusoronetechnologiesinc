╔═══════════════════════════════════════════════════════════════════════════════╗
║ WINDSURF INSTRUCTION                                                          ║
╠═══════════════════════════════════════════════════════════════════════════════╣
║ TEAM:           Beta                                                          ║
║ PRIORITY:       P1 — HIGH                                                     ║
║ CATEGORY:       Frontend                                                      ║
║ ID:             WS-P1-010                                                     ║
╚═══════════════════════════════════════════════════════════════════════════════╝

## ISSUE
Implement empty states across all pages. List pages show blank content when no data exists. Empty component exists (components/ui/empty.tsx) but isn't used.

## AFFECTED USER TYPES
- All users

## FILE(S) TO MODIFY
- All list and dashboard pages

## REQUIRED CHANGE

**Pattern (use existing Empty component):**
```typescript
import Empty from '../components/ui/empty';

const [data, setData] = useState([]);
const [loading, setLoading] = useState(true);

return (
  <div>
    {loading ? (
      <Spinner />
    ) : data.length === 0 ? (
      <Empty
        title="No loads found"
        description="Create your first load to get started"
        action={{ label: 'Create Load', href: '/loads/new' }}
      />
    ) : (
      <LoadsList data={data} />
    )}
  </div>
);
```

**Apply to all list pages with role-appropriate CTAs:**
- ShipperLoads.tsx: "Create your first load"
- CatalystBids.tsx: "No bids yet — browse available loads"
- ComplianceReports.tsx: "No reports — run first compliance check"
- DispatchAssignments.tsx: "No assignments — create dispatch"

## VERIFICATION
1. Create new user with no data
2. Navigate to list pages → see helpful empty state with CTA
3. Verify blank pages are replaced with useful guidance
4. Click CTA button → navigate to creation page

## CONSENSUS
Beta
