╔═══════════════════════════════════════════════════════════════════════════════╗
║ WINDSURF INSTRUCTION                                                          ║
╠═══════════════════════════════════════════════════════════════════════════════╣
║                                                                               ║
║ TEAM:           Team Alpha (Backend) + ALL TEAMS                              ║
║ PRIORITY:       P0 — CRITICAL                                                 ║
║ CATEGORY:       Test Data / User Accounts                                     ║
║ ID:             WS-P0-011                                                     ║
║                                                                               ║
╚═══════════════════════════════════════════════════════════════════════════════╝

## ISSUE

5 of 12 user types have no test accounts: CARRIER, ADMIN, COMPLIANCE_OFFICER, SAFETY_MANAGER, FACTORING. Cannot test their workflows at all. (Note: In EusoTrip, CATALYST is the dispatcher/carrier role.)

## REQUIRED CHANGE

Create a seed script or database inserts:

```sql
-- Create missing user accounts
-- (Adjust password hash to match your auth system, or use OAuth-only)

INSERT INTO users (email, name, role, isActive, isVerified, companyId, createdAt) VALUES
('carrier@eusotrip.com', 'Test Carrier User', 'CARRIER', true, true, NULL, NOW()),
('admin@eusotrip.com', 'Test Admin', 'ADMIN', true, true, NULL, NOW()),
('compliance@eusotrip.com', 'Test Compliance Officer', 'COMPLIANCE_OFFICER', true, true, NULL, NOW()),
('safety@eusotrip.com', 'Test Safety Manager', 'SAFETY_MANAGER', true, true, NULL, NOW()),
('factoring@eusotrip.com', 'Test Factoring Agent', 'FACTORING', true, true, NULL, NOW());

-- Note: companyId will be set in WS-P0-012 after carrier company is created
-- Note: wallets will auto-create on first access via ensureWallet()
```

Also verify the existing test users have proper configuration:
- shipper@eusotrip.com → SHIPPER, companyId set
- catalyst@eusotrip.com → CATALYST, needs companyId
- broker@eusotrip.com → BROKER, needs companyId
- driver@eusotrip.com → DRIVER, needs companyId
- escort@eusotrip.com → ESCORT
- terminal@eusotrip.com → TERMINAL_MANAGER
- superadmin@eusotrip.com → SUPER_ADMIN

## VERIFICATION

```sql
SELECT role, COUNT(*) as count FROM users GROUP BY role ORDER BY role;
-- Should show all 12 roles with at least 1 user each
```

MCP: `list_users({ limit: 25 })` → all 12 roles present

## CONSENSUS

ALL TEAMS — this unblocks testing for every team
